# מדריך עריכת תוכן — אתר ההנצחה

כל התוכן של האתר נמצא בקבצים פשוטים. אין מסד נתונים, אין התחברות —
עורכים קובץ, שומרים, דוחפים ל-GitHub, והאתר מתעדכן אוטומטית תוך כמה דקות.

## איפה נמצא כל דבר

| מה רוצים לשנות | איזה קובץ לערוך |
|---|---|
| שם, תאריכים, ציטוטים, תקציר בדף הבית | `src/content/profile.ts` |
| פרקי סיפור החיים והתמונות שבתוכם | `src/content/story.ts` |
| ציר הזמן | `src/content/timeline.ts` |
| אלבומי הגלריה | `src/content/gallery.ts` |
| מילים לזכרו (הספדים ומכתבים) | `src/content/words.ts` |
| קבצי התמונות עצמם | התיקייה `public/images/` |

## איך מוסיפים תמונות

**חשוב: להקטין את התמונות לפני ההעלאה!** האתר נצפה בבית העלמין,
לרוב עם קליטה סלולרית חלשה. תמונה ישירה מהטלפון שוקלת 3–5MB — זה איטי מדי.

1. היכנסו לאתר [squoosh.app](https://squoosh.app) (חינמי, עובד בדפדפן)
2. גררו את התמונה פנימה
3. בצד שמאל: Resize → רוחב **1200** פיקסלים
4. פורמט: **WebP** או JPEG, איכות בסביבות 75
5. הורידו — הקובץ אמור לשקול פחות מ-300KB
6. שמרו את הקובץ בתיקייה המתאימה:
   - תמונות של סיפור החיים → `public/images/story/`
   - תמונות גלריה → `public/images/gallery/<שם-האלבום>/`
   - תמונת הפרופיל בדף הבית → `public/images/` (ועדכנו את `photo` ב-profile.ts)
7. הוסיפו שורה בקובץ התוכן המתאים, למשל ב-story.ts:

```ts
images: [
  { src: 'images/story/childhood-1.webp', caption: 'מאיר בילדותו בעכו' },
],
```

## הרבה תמונות סרוקות בבת אחת

כשיש תיקייה שלמה של סריקות, אין צורך לעבור תמונה-תמונה ב-squoosh.
יש סקריפט שעושה את הכול: מיישר אוריינטציה, חותך את שולי הסורק הלבנים,
מיישר תמונות שנסרקו עקום, מקטין ל-1200px ודוחס מתחת ל-300KB.

```bash
npm install                      # פעם אחת, מתקין את הכלים
npm run photos -- --in "C:\Users\shait\OneDrive\Pictures\מאיר תמם" --out images/gallery/army
```

`--in` היא תיקיית הסריקות במחשב, `--out` הוא היעד בתוך `public/`.
בסוף הריצה נוצר **דף בדיקה** ב-`photo-review/` — פותחים אותו בדפדפן,
רואים את כל התמונות אחרי העיבוד, ומסובבים ידנית כל תמונה ששוכבת על הצד
(את זה המחשב לא יודע לזהות לבד). לוחצים «שמירת קובץ הסיבובים», שמים את
`rotations.json` בתיקיית הפרויקט ומריצים שוב את אותה פקודה עם:

```bash
npm run photos -- --in "..." --out images/gallery/army --rotations rotations.json
```

בתחתית דף הבדיקה יש גם שורות מוכנות להעתקה לתוך קובץ התוכן.

אפשרויות שימושיות נוספות:

| אפשרות | מה היא עושה |
|---|---|
| `--split` | סריקה שמכילה כמה תמונות על אותו משטח — מפצלת לקבצים נפרדים |
| `--dry` | ריצת ניסיון: מדפיסה מה יקרה בלי לכתוב קבצים |
| `--recursive` | סורקת גם תת-תיקיות |
| `--max 1000` | רוחב מרבי אחר (ברירת מחדל 1200) |
| `--format jpeg` | פלט JPEG במקום WebP |

הסקריפט לעולם לא נוגע בקבצי המקור — הוא רק קורא מהם וכותב עותקים חדשים.
אם הוא לא בטוח בחיתוך, הוא שומר את התמונה שלמה ומציין את זה בדוח.

## איך מוסיפים פרק לסיפור החיים

פתחו את `src/content/story.ts` והעתיקו את התבנית:

```ts
{
  id: 'my-chapter',            // מזהה באנגלית, ללא רווחים
  title: 'כותרת הפרק',
  paragraphs: [
    'הפסקה הראשונה...',
    'הפסקה השנייה...',
  ],
  images: [
    { src: 'images/story/photo.webp', caption: 'כיתוב לתמונה' },
  ],
},
```

התמונות משתלבות אוטומטית בין הפסקאות, לאורך הפרק.

## איך מוסיפים הספד ב"מילים לזכרו"

פתחו את `src/content/words.ts` והוסיפו:

```ts
{
  author: 'שם הכותב',
  relation: 'הבן',           // אופציונלי
  paragraphs: [
    'הטקסט...',
  ],
},
```

## איך מפרסמים את השינויים

```bash
git add .
git commit -m "עדכון תוכן"
git push
```

הפריסה ל-GitHub Pages רצה אוטומטית (2–3 דקות). אפשר לעקוב בלשונית Actions.

## הגדרה חד-פעמית של GitHub Pages

פעם אחת בלבד, בהגדרות המאגר ב-GitHub:
**Settings → Pages → Source → GitHub Actions**

לאחר מכן האתר יהיה זמין בכתובת:
`https://<שם-המשתמש>.github.io/Meir_Tamam_Memorial/`

זו הכתובת שאליה מפנים את קוד ה-QR שעל המצבה.
מומלץ להזמין את שלט ה-QR רק אחרי שהאתר עלה ונבדק בטלפון.

## בדיקה מקומית לפני פרסום

```bash
npm install        # פעם ראשונה בלבד
npm run dev        # שרת פיתוח — כתובת מקומית תופיע בטרמינל
npm run build      # בדיקה שהכל תקין לפני push
```
